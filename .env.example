# GEMINI_API_KEY: Optional if using Gemini features
GEMINI_API_KEY=""

# DATABASE_URL: PostgreSQL Connection String (or Neon / Supabase / Local Postgres)
# If left empty, the application will automatically run with an in-memory database store.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/myfinancedb"
